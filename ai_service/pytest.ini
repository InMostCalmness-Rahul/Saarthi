[pytest]
# Tests import the service modules directly (ai_engine, config, schemas), so the
# service directory must be importable when pytest is invoked from ai_service/.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
