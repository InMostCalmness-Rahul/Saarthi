# Development / CI tooling for the AI service (not needed at runtime).
-r requirements.txt
pytest>=7.4.0,<9.0.0
ruff>=0.6.0,<1.0.0
# Required by fastapi.testclient for the ASGI-level contract tests.
httpx>=0.27.0,<1.0.0
