# Optional. Leave unset for local development: the app then calls the backend on
# relative URLs and Vite proxies /api to http://127.0.0.1:5000.
#
# For a deployed build, set the public backend origin, e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
